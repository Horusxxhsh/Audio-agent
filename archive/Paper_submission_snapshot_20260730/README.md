# Paper submission snapshot — 2026-07-30

This is a non-destructive archive of the former top-level `Paper_submission/`
tree and `Paper_submission.zip`.

- `Paper_submission/` preserves the original directory exactly as moved.
- `Paper_submission_snapshot_20260730.zip` preserves the original root-level
  archive, including its `Paper_submission/` top-level entry.
- The active editable manuscript is `Paper/`; the matching upload package is
  `Paper/submission/`.

The archived `source/content.tex` intentionally differs from the source inside
its sibling upload zip. This snapshot is retained for provenance, not as an
active submission package. No content was edited while creating the archive.
