# Legacy Paper Snapshot

This directory stores the previous `Paper/` contents before the current TMM
major revision workspace was promoted from `Paper_submission/` to `Paper/`.

The snapshot mainly contains compiled PDFs, LaTeX auxiliary files, response
PDFs, and figure outputs from the earlier paper state. It is retained only for
traceability. The active manuscript source is now `Paper/`.
