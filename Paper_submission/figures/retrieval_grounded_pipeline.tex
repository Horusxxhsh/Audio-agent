\begin{tikzpicture}[
    node distance=0.7cm and 0.9cm,
    font=\small,
    io/.style={ellipse, draw, thick, align=center, minimum width=2.3cm, minimum height=0.95cm, fill=blue!8},
    stage/.style={rounded rectangle, draw, thick, align=center, minimum width=2.7cm, minimum height=1.05cm, fill=gray!8},
    detail/.style={rounded rectangle, draw, thick, align=center, minimum width=2.8cm, minimum height=0.95cm, fill=orange!12},
    group/.style={rounded rectangle, draw, thick, inner sep=0.35cm, fill=white},
    flow/.style={-{Latex[length=2.5mm]}, thick},
    asyncflow/.style={-{Latex[length=2.5mm]}, thick, dashed},
]

\node[io] (textq) {Text query};
\node[io, below=1.1cm of textq] (audioq) {Audio reference};

\node[detail, right=1.7cm of textq] (textretr) {Text retrieval};
\node[detail, below=1.1cm of textretr] (trr) {TRR encoding\\+ audio retrieval};
\node[group, fit=(textretr)(trr), label={[font=\small]north:Retrieval module}] (retrgroup) {};

\node[stage, right=1.4cm of retrgroup] (select) {Top-$K$ selection\\and validation};
\node[stage, right=1.5cm of select] (project) {Deterministic\\projection};
\node[stage, right=1.5cm of project] (dsp) {Real-time DSP\\engine};
\node[io, right=1.6cm of dsp] (output) {Editable\\audio output};

\draw[flow] (textq.east) -- (textretr.west);
\draw[flow] (audioq.east) -- (trr.west);
\draw[flow] (textretr.east) -- (select.west);
\draw[flow] (trr.east) -- (select.west);
\draw[flow] (select.east) -- (project.west);
\draw[asyncflow] (project.east) -- node[above, font=\scriptsize] {async parameter update} (dsp.west);
\draw[flow] (dsp.east) -- (output.west);

\end{tikzpicture}
